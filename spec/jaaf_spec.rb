# frozen_string_literal: true

RSpec.describe Jaaf do
  it "has a version number" do
    expect(Jaaf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
