- if icon.present?
  = helpers.link_to path, **options.merge(data: { turbo_frame: '_top' }, class: 'dropdown-item') do
    i.bi.me-2 class=icon_class
    = label
- else
  = helpers.link_to label, path, **options.merge(data: { turbo_frame: '_top' }, class: 'dropdown-item')